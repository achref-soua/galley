//! Galley version-control adapter — placeholder crate.
//!
//! Will wrap `git2`/libgit2 to provide auto-checkpoints, named snapshots, a
//! diffable timeline, revert, and the autonomous-mode shadow branch, behind the
//! `VersionControl` port. No functionality yet — this is the v0.0.1 scaffold.
