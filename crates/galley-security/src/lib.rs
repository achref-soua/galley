//! Galley security primitives — placeholder crate.
//!
//! Will host the OS-keychain secret store (`keyring`), the compile/import
//! sandbox policy (shell-escape off by default, path-traversal guards), and the
//! input sanitizers, behind the `SecretStore` port and the sandbox traits. No
//! functionality yet — this is the v0.0.1 scaffold.
