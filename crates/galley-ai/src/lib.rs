//! Galley AI subsystem — placeholder crate.
//!
//! Will host the provider-agnostic `LlmProvider` adapters (cloud + local), the
//! multi-agent orchestrator, and the MCP tool host that exposes a constrained,
//! sandboxed toolset to the agents. Provider-neutral by design — no assistant is
//! branded or default-enabled. No functionality yet — this is the v0.0.1 scaffold.
