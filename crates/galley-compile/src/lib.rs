//! Galley compile adapters — placeholder crate.
//!
//! Will host the embedded Tectonic engine (kept warm in process, backed by an
//! in-memory VFS) and the opt-in `latexmk`/TeX Live fallback, both behind the
//! `Compiler` port. No functionality yet — this is the v0.0.1 scaffold.
