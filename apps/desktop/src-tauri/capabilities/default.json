{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview access for the main window, plus native folder-picker dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
