# @galley/ui-kit

Galley's design system: the typewriter-inspired design tokens, the Onionskin and
Carbon themes, and the shared Svelte components.

In **v0.0.1** this package ships only the design tokens (`tokens.css`). The
themes, primitives, and Storybook arrive in **v0.0.2 — Design system & themes**.
